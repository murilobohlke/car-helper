import 'package:flutter/material.dart';

class InfoCarGalleryPage extends StatelessWidget {
  const InfoCarGalleryPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('Galeria'),),
    );
  }
}